> # ⚠️ Questo repository è un archivio (dal 16/09/2026)
>
> **Il codice di IAM vive nel repository unico:**
> [`francescotp93/QUOTE`](https://github.com/francescotp93/QUOTE), cartella **`iam/`**
> (il repository si chiamerà `IAM`; GitHub rimanda dal vecchio nome).
> L'ultimo commit di qui importato là è `e5b84ac`. Da quel momento **niente di
> quello che si scrive qui arriva in produzione**: `iam.withusassicurazioni.it`
> è servito dal VPS da `/opt/withus-backend/iam`, che segue il `main` del
> repository unico.
>
> Questo repository resta in sola lettura per la storia dei commit e delle PR.
> Le regole e i documenti condivisi (`IAM.md`, `WORKFLOW.md`,
> `INTERFACCIA-QUOTO-IAM.md`) stanno una volta sola, alla radice del repository
> unico.

# Agente-sospesi